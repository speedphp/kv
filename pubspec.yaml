name: kv
description:  Another Key-Value storage for Dart.
version: 1.0.0
repository: https://github.com/speedphp/kv
homepage: https://github.com/SpeedPHP/kv

environment:
  sdk: ^3.1.1

platforms:
  linux:
  windows:
  macos:

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
